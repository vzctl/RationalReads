<div class="head">
  <h1><%= work.escape("name") %></h1>
  <% if (mod === "1") { %>
    <a class="filter-link" href="/#/works/<%= work.id %>/edit">Edit</a>
  <% } %>
</div>

<div style="clear:both">
</div>

<p class="author"> by <%= work.escape("author") %> </p>

<div class="stats">
  <div class="num-ratings">
    <% if (work.escape("ratings") == "1")  { %>
      <div class="num-ratings-number"><%= work.escape("ratings") %></div> Rating
    <% } else { %>
      <div class="num-ratings-number"><%= work.escape("ratings") %></div> Ratings
    <% } %>
  </div>

  <div class="average-rating" title="This is a non-bayesian average rating, meaning that it does not take into account the number of votes this work has received.">
    <span class="other-avg"><%= parseFloat(work.escape("average_rating")).toFixed(2) %> Average  /</span>
  </div>

  <div class="bayesian-rating" title="Accounts for the number of ratings, so that a work with a single five star rating doesn't hit the top of the highest rated list.">
    <span class="avg"><%= parseFloat(work.escape("bayesian_average")).toFixed(2) %> Bayesian Adjusted</span>
  </div>

  <div>
    Your Rating:
  </div>
  <div class="stars">
    <div class="raty"></div>
  </div>
</div>

<div class="rating-bars">
  <div class="rating-bar-block">
    <span>5 |</span>
    <div class="rating-bar fives"></div>
    <% if (work.escape("fives") != "0") { %>
      <span><%= work.escape("fives") %></span>
    <% } %>
  </div>
  <div class="rating-bar-block">
    <span>4 |</span>
    <div class="rating-bar fours"></div>
    <% if (work.escape("fours") != "0") { %>
      <%= work.escape("fours") %>
    <% } %>
  </div>
  <div class="rating-bar-block">
    <span>3 |</span>
    <div class="rating-bar threes"></div>
    <% if (work.escape("threes") != "0") { %>
      <%= work.escape("threes") %>
    <% } %>
  </div>
  <div class="rating-bar-block">
    <span>2 |</span>
    <div class="rating-bar twos"></div>
    <% if (work.escape("twos") != "0") { %>
      <%= work.escape("twos") %>
    <% } %>
  </div>
  <div class="rating-bar-block">
    <span>1 |</span>
    <div class="rating-bar ones"></div>
    <% if (work.escape("ones") != "0") { %>
      <%= work.escape("ones") %>
    <% } %>
  </div>
</div>

<div class="non-rating stats">
  <div class="num-comments">
    <% if (work.escape("num_comments") == "1")  { %>
      <span class="num-comments-number"><%= work.escape("num_comments") %></span> Comment
    <% } else { %>
      <span class="num-comments-number"><%= work.escape("num_comments") %></span> Comments
    <% } %>
  </div>
  <div class="date">
    Added on <%= work.escape("created_at_string") %>
  </div>
  <div class="length" title="Short works have less than 25k words, medium works less than 100k, and long works less than 500k. Epic works, are well, epic.">
    <%= work.escape("length") %> Length
  </div>
</div>


<div class="tag-display-box">
  <% work.get("tags").forEach( function (tag) { %>
    <div class="single-tag"><p><%= tag %></p></div>
  <% }) %>
</div>

<p class="description"><%= work.escape("description") %></p>
Read it <a href="<%= work.escape("link") %>">here</a>.
